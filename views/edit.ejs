<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen flex flex-col items-center justify-start pb-20" style="background: linear-gradient(135deg, #f6d365 0%, #fda085 100%); background-attachment: fixed;"></body>
  <a href="/logout" class="fixed top-6 right-1 z-50 px-5 py-2 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg shadow-lg text-sm transition duration-200 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-red-400 focus:ring-offset-2">
    Logout
  </a>
  <div class="w-full max-w-md mt-14 rounded-xl bg-white shadow-md p-10">
    <h1 class="text-xl font-bold text-gray-800 mb-2 ">Update your post</h1>
    <form action="/update/<%= post._id %>" method="POST">
      <div class="mb-4">
        <label for="title" class="block text-sm font-medium text-gray-700">Title</label>
        <input id="title" name="title" type="text" required class="appearance-none border-2 border-gray-300 w-full py-2 px-3 text-gray-700 rounded-md focus:outline-none focus:border-blue-500" placeholder="Title" value="<%= post.title %>">
        
        <label for="content" class="block text-sm font-medium text-gray-700 mt-2">Content</label>
        <textarea id="content" name="content" required class="appearance-none border-2 border-gray-300 resize-none w-full py-4 px-3 text-gray-700" placeholder="Content"><%=post.content %></textarea>
        
        <input type="submit" value="Update" class="w-full mt-4 bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
      </div>
    </form>
  </div>
</body>
</html>
